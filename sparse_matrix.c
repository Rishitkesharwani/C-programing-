#include<stdio.h>

int main()
{
    int a,b
    printf("Enter the size of matrix:");
    scanf("%d %d",&a,&b);
    printf("Enter the Element of Matrix:");
    for (int i=0;i<n;i++)
    {
        for(int j=o;j<m;j++)
        {
            scanf("%d",a[i][j])
        }
        printf("\n");
    }
    int count=0;
    

    return 0;
}